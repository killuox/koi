Procedures for releasing a new version on npm:
    1. Build the project: goreleaser build --snapshot --clean
    2. Place the generated files in dist to the bin folder
    3. Commit the changes
    4. npm version patch
    5. npm publish
