- Add better validation for missing key in the configs ex: if no method for endpoint is provided the error is unclear - use github.com/go-playground/validator/v10
- Add help output when empty args and help flags
- Try to deploy + download(goreleaser + node wrapper + homebrew + scoop + go)
- Make a documentation(docusorus) website + branding + logo 

IMPROVEMENTS
- Refactoring(try to get rid of the shared.go)
- Remove all any(s) and find more robust solutions
- Improve all error messages
- Make random values when no rules provided
- Improve Parameters struct depending on mode
- Fix loader still showing in the terminal after

EXTRA FEATURES
- Add tags to display headers or other informations
- More rules?

