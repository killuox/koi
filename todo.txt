Check how to read yaml file
    - to test in prod once deployed
Figure out yaml file structure + config
    - think about how to handle auth
    - mode: default, seed, env, ai(future)
    - CORS
Brainstorm commands structure
    - if no arg prodived it will be the help command
    - else its a endpoint call
--------------
- Fix loader still showing in the terminal after <-
- Handle all endpoint method and specifications
    - path X
    - query X
    - DELETE X
    - POST X
    - PATCH
    - PUT
- EMV mode
- Seed mode
- Add help output when empty args
- Add tags to display headers
- Check if endpoint required rule is necessary
